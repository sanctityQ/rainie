/**
 * Generated by Scrooge
 *   version: 4.5.0
 *   rev: 014664de600267b36809bbc85225e26aec286216
 *   built at: 20160203-205352
 */
package com.itiancai.passport.thrift

import com.twitter.finagle.Thrift
import com.twitter.finagle.stats.{NullStatsReceiver, StatsReceiver}
import com.twitter.scrooge.{ThriftStruct, TReusableMemoryTransport}
import com.twitter.util.{Future, Return, Throw, Throwables}
import java.nio.ByteBuffer
import java.util.Arrays
import org.apache.thrift.protocol._
import org.apache.thrift.TApplicationException
import org.apache.thrift.transport.TMemoryInputTransport
import scala.collection.mutable.{
  ArrayBuffer => mutable$ArrayBuffer, HashMap => mutable$HashMap}
import scala.collection.{Map, Set}

import scala.language.higherKinds


@javax.annotation.Generated(value = Array("com.twitter.scrooge.Compiler"))
class PassportService$FinagleService(
  iface: PassportService[Future],
  protocolFactory: TProtocolFactory,
  stats: StatsReceiver,
  maxThriftBufferSize: Int,
  serviceName: String
) extends com.twitter.finagle.Service[Array[Byte], Array[Byte]] {
  import PassportService._

  def this(
    iface: PassportService[Future],
    protocolFactory: TProtocolFactory,
    stats: StatsReceiver,
    maxThriftBufferSize: Int
  ) = this(iface, protocolFactory, stats, maxThriftBufferSize, "PassportService")

  def this(
    iface: PassportService[Future],
    protocolFactory: TProtocolFactory
  ) = this(iface, protocolFactory, NullStatsReceiver, Thrift.maxThriftBufferSize)

  private[this] val tlReusableBuffer = new ThreadLocal[TReusableMemoryTransport] {
    override def initialValue() = TReusableMemoryTransport(512)
  }

  private[this] def reusableBuffer: TReusableMemoryTransport = {
    val buf = tlReusableBuffer.get()
    buf.reset()
    buf
  }

  private[this] val resetCounter = stats.scope("buffer").counter("resetCount")

  private[this] def resetBuffer(trans: TReusableMemoryTransport): Unit = {
    if (trans.currentCapacity > maxThriftBufferSize) {
      resetCounter.incr()
      tlReusableBuffer.remove()
    }
  }

  protected val functionMap = new mutable$HashMap[String, (TProtocol, Int) => Future[Array[Byte]]]()

  protected def addFunction(name: String, f: (TProtocol, Int) => Future[Array[Byte]]) {
    functionMap(name) = f
  }

  protected def exception(name: String, seqid: Int, code: Int, message: String): Future[Array[Byte]] = {
    try {
      val x = new TApplicationException(code, message)
      val memoryBuffer = reusableBuffer
      try {
        val oprot = protocolFactory.getProtocol(memoryBuffer)

        oprot.writeMessageBegin(new TMessage(name, TMessageType.EXCEPTION, seqid))
        x.write(oprot)
        oprot.writeMessageEnd()
        oprot.getTransport().flush()
        Future.value(Arrays.copyOfRange(memoryBuffer.getArray(), 0, memoryBuffer.length()))
      } finally {
        resetBuffer(memoryBuffer)
      }
    } catch {
      case e: Exception => Future.exception(e)
    }
  }

  protected def reply(name: String, seqid: Int, result: ThriftStruct): Future[Array[Byte]] = {
    try {
      val memoryBuffer = reusableBuffer
      try {
        val oprot = protocolFactory.getProtocol(memoryBuffer)

        oprot.writeMessageBegin(new TMessage(name, TMessageType.REPLY, seqid))
        result.write(oprot)
        oprot.writeMessageEnd()

        Future.value(Arrays.copyOfRange(memoryBuffer.getArray(), 0, memoryBuffer.length()))
      } finally {
        resetBuffer(memoryBuffer)
      }
    } catch {
      case e: Exception => Future.exception(e)
    }
  }

  final def apply(request: Array[Byte]): Future[Array[Byte]] = {
    val inputTransport = new TMemoryInputTransport(request)
    val iprot = protocolFactory.getProtocol(inputTransport)

    try {
      val msg = iprot.readMessageBegin()
      val func = functionMap.get(msg.name)
      func match {
        case _root_.scala.Some(fn) =>
          fn(iprot, msg.seqid)
        case _ =>
          TProtocolUtil.skip(iprot, TType.STRUCT)
          exception(msg.name, msg.seqid, TApplicationException.UNKNOWN_METHOD,
            "Invalid method name: '" + msg.name + "'")
      }
    } catch {
      case e: Exception => Future.exception(e)
    }
  }

  // ---- end boilerplate.

  private[this] val scopedStats = if (serviceName != "") stats.scope(serviceName) else stats
  private[this] object __stats_userValidate {
    val RequestsCounter = scopedStats.scope("userValidate").counter("requests")
    val SuccessCounter = scopedStats.scope("userValidate").counter("success")
    val FailuresCounter = scopedStats.scope("userValidate").counter("failures")
    val FailuresScope = scopedStats.scope("userValidate").scope("failures")
  }
  addFunction("userValidate", { (iprot: TProtocol, seqid: Int) =>
    try {
      __stats_userValidate.RequestsCounter.incr()
      val args = UserValidate.Args.decode(iprot)
      iprot.readMessageEnd()
      (try {
        iface.userValidate(args.name, args.value)
      } catch {
        case e: Exception => Future.exception(e)
      }).flatMap { value: Boolean =>
        reply("userValidate", seqid, UserValidate.Result(success = Some(value)))
      }.rescue {
        case e => Future.exception(e)
      }.respond {
        case Return(_) =>
          __stats_userValidate.SuccessCounter.incr()
        case Throw(ex) =>
          __stats_userValidate.FailuresCounter.incr()
          __stats_userValidate.FailuresScope.counter(Throwables.mkString(ex): _*).incr()
      }
    } catch {
      case e: TProtocolException => {
        iprot.readMessageEnd()
        exception("userValidate", seqid, TApplicationException.PROTOCOL_ERROR, e.getMessage)
      }
      case e: Exception => Future.exception(e)
    }
  })
  private[this] object __stats_reg {
    val RequestsCounter = scopedStats.scope("reg").counter("requests")
    val SuccessCounter = scopedStats.scope("reg").counter("success")
    val FailuresCounter = scopedStats.scope("reg").counter("failures")
    val FailuresScope = scopedStats.scope("reg").scope("failures")
  }
  addFunction("reg", { (iprot: TProtocol, seqid: Int) =>
    try {
      __stats_reg.RequestsCounter.incr()
      val args = Reg.Args.decode(iprot)
      iprot.readMessageEnd()
      (try {
        iface.reg(args.request)
      } catch {
        case e: Exception => Future.exception(e)
      }).flatMap { value: com.itiancai.passport.thrift.RegResponse =>
        reply("reg", seqid, Reg.Result(success = Some(value)))
      }.rescue {
        case e => Future.exception(e)
      }.respond {
        case Return(_) =>
          __stats_reg.SuccessCounter.incr()
        case Throw(ex) =>
          __stats_reg.FailuresCounter.incr()
          __stats_reg.FailuresScope.counter(Throwables.mkString(ex): _*).incr()
      }
    } catch {
      case e: TProtocolException => {
        iprot.readMessageEnd()
        exception("reg", seqid, TApplicationException.PROTOCOL_ERROR, e.getMessage)
      }
      case e: Exception => Future.exception(e)
    }
  })
  private[this] object __stats_userInfo {
    val RequestsCounter = scopedStats.scope("userInfo").counter("requests")
    val SuccessCounter = scopedStats.scope("userInfo").counter("success")
    val FailuresCounter = scopedStats.scope("userInfo").counter("failures")
    val FailuresScope = scopedStats.scope("userInfo").scope("failures")
  }
  addFunction("userInfo", { (iprot: TProtocol, seqid: Int) =>
    try {
      __stats_userInfo.RequestsCounter.incr()
      val args = UserInfo.Args.decode(iprot)
      iprot.readMessageEnd()
      (try {
        iface.userInfo()
      } catch {
        case e: Exception => Future.exception(e)
      }).flatMap { value: Boolean =>
        reply("userInfo", seqid, UserInfo.Result(success = Some(value)))
      }.rescue {
        case e => Future.exception(e)
      }.respond {
        case Return(_) =>
          __stats_userInfo.SuccessCounter.incr()
        case Throw(ex) =>
          __stats_userInfo.FailuresCounter.incr()
          __stats_userInfo.FailuresScope.counter(Throwables.mkString(ex): _*).incr()
      }
    } catch {
      case e: TProtocolException => {
        iprot.readMessageEnd()
        exception("userInfo", seqid, TApplicationException.PROTOCOL_ERROR, e.getMessage)
      }
      case e: Exception => Future.exception(e)
    }
  })
}